<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tpl="clr-namespace:QueueApp.Shared.Templates"
             x:Name="Root"
             VerticalOptions="Start"
             x:Class="QueueApp.Features.CategoryPicker.Helpers.LocationBarView">

    <Border Style="{StaticResource brd16_Surface}" HeightRequest="52" Padding="14,0">
        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">

            <tpl:ThemedIcon Grid.Column="0" Icon="ic_pin" WidthRequest="16" HeightRequest="16"
                   VerticalOptions="Center"/>

            <Label x:Name="LocationLabel" Grid.Column="1"
                   Text="{Binding Text, Source={x:Reference Root}}"
                   Style="{Binding TextStyle, Source={x:Reference Root}}"
                   LineBreakMode="TailTruncation" MaxLines="1"
                   VerticalOptions="Center"/>

            <tpl:ThemedIcon Grid.Column="2" Icon="{Binding TrailingIcon, Source={x:Reference Root}}"
                   WidthRequest="14" HeightRequest="14" Opacity="0.65"
                   IsVisible="{Binding ShowTrailingIcon, Source={x:Reference Root}}"
                   VerticalOptions="Center"/>

        </Grid>

        <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding RefreshCommand, Source={x:Reference Root}}"/>
        </Border.GestureRecognizers>
    </Border>

</ContentView>
