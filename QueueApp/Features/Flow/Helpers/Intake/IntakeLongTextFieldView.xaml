<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="Root"
             x:Class="QueueApp.Features.Flow.Helpers.Intake.IntakeLongTextFieldView">

    <Border Padding="10,4" Style="{StaticResource brd12_Cream}">
        <Editor Text="{Binding Text, Source={x:Reference Root}}"
                Placeholder="{Binding Placeholder, Source={x:Reference Root}}"
                Style="{StaticResource editor14_Sheet}"/>
    </Border>

</ContentView>
